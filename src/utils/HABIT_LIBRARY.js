// src/utils/HABIT_LIBRARY.js
// Legacy hardcoded catalog intentionally removed.
// Migrate to: boot.catalog.habits (HabitProvider.catalogHabits / getCatalogHabit)

throw new Error(
  "[HABIT_LIBRARY REMOVED] Migrate to server catalog (boot.catalog.habits) and delete this import."
);
